export const environment = {
    production: false,
    // apiUrl: 'https://localhost:5001/api/', // HTTPS with SSL certificate if not use docker. see the launchSettings.json
    apiUrl: 'http://localhost:5000/api/'

};
